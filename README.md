# Analytical Models

This assignment is designed to help students walk through analytical models.  The repository contains a packege named data_generator that should work with a basic python instlal with pandas and matplotlib. This folder was generated by the Scientific Software Project Template and has a makefile that can be used to explore tools. 

Start with the command:

```make init```

